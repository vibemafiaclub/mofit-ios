# User Intervention — 인간 개입이 필요한 운영 절차

cc-system `plan-and-build` skill 원칙: **모든 구현은 CLI + AI 에이전트로 완결되어야 한다.** 그럼에도 불가피하게 사용자(사람)가 직접 수행해야 하는 절차는 여기에 기록해 재사용 가능한 체크리스트로 만든다.

각 항목은 다음을 명시한다:

- **언제 필요한가** (트리거 조건)
- **사용자가 할 일** (단계별)
- **누가 에이전트를 재개하는가** (완료 신호)

---

## 0. 템플릿

```markdown
### <제목>

- **트리거**: 이 개입이 필요한 상황 한 문장.
- **이유**: 왜 CLI/에이전트로 자동화할 수 없는지 (웹 UI 종속, 외부 서비스 OAuth, 물리적 동작 등).
- **절차**:
  1. 사용자가 할 첫 단계.
  2. 사용자가 할 두 번째 단계.
- **재개 신호**: 사용자가 에이전트에게 "X 완료" 라고 알려주면 에이전트가 다음 단계 진행.
- **기록 위치**: 완료 시각/값을 남길 곳 (예: iteration requirement.md 의 intervention 섹션).
```

---

## 현재 등록된 절차

_(없음 — 하네스가 실행되며 필요한 개입이 발견될 때마다 plan-and-build skill 이 이 파일에 항목을 추가한다.)_

---

## 주의

- **Secrets**: 새 API 키가 필요한 경우 `Mofit/Config/Secrets.swift` 또는 `server/.env` 를 직접 편집해야 할 수 있다. 두 파일 모두 `.gitignore` 이므로 에이전트도 Read 로만 접근해야 한다.
- **App Store 제출·심사**: TestFlight·App Store Connect 는 웹 UI 전담이라 자동화 불가. 릴리즈 단계 도달 시 이 문서에 상세 절차 추가 예정.
- **Supabase 스키마 변경**: SQL 마이그레이션은 CLI 가능하지만, 프로덕션 DB 직접 변경은 사용자 확인 필수.

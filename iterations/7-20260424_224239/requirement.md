# Requirement

## 가치제안

---
run_id: home-workout-newbie-20s_20260424_224420
persona_id: home-workout-newbie-20s
persona_version: 1
delivery_mode: landing_plus_meeting
created_at: 2026-04-24T22:44:20
---

# Mofit — iPhone 카메라만으로 스쿼트를 자동으로 세는 앱

## 1. 가치제안 요약 (랜딩페이지 카피)

### 메인 카피

**"폰 세워두고 손바닥 1초 또는 화면 탭. 스쿼트가 자동으로 세어집니다."**

iPhone 카메라만으로 스쿼트를 자동으로 세고, 관절 각도로 실시간 자세를 보여줍니다. 별도 장비·회원가입 없이, 앱 켠 지 1분 안에 첫 카운트가 올라갑니다.

### 서브 카피

- **스쿼트 전용 자동 카운터**: Apple Vision으로 hip–knee–ankle 각도 분석. 서있음(>160°) → 앉음(<100°) → 서있음 = 1 rep 인정. 분석 15fps 샘플링(배터리 절약 + 스쿼트 속도에 충분).
- **로그인 없이 바로 시작**: 설치 → 온보딩 → 즉시 카운트. 기록은 내 폰 로컬(SwiftData)에만 저장, 외부 전송 0.
- **시작·종료 트리거 이중화**: 손바닥 1초 OR **화면 탭**. 손바닥 인식이 안 되는 각도/조도에서도 탭 한 번으로 카운트다운 시작.
- **안 세어지는 이유를 인앱에서 바로 안내 (ADR-018)**: 트래킹 중 양쪽 다리가 프레임에 안 잡히거나, 조도가 낮아 하체 조인트 평균 confidence가 임계치 미만으로 3초 지속되면 상단에 반투명 배너로 "전신이 프레임에 들어오는지 확인하세요" / "조명이 어두울 수 있어요" 힌트 표시. 5초 grace 후, rep이 한 번이라도 카운트되면 이후 숨김 (주의 분산 방지).
- **rep 단위 autosave (ADR-009 2026-04-24 update, 신규)**: 세트 시작 시점에 `WorkoutSession` 을 즉시 로컬 DB 에 insert, 매 rep 이 카운트될 때마다 `repCounts` 스냅샷을 `modelContext.save()`. 운동 중 전화·앱 전환·배터리 방전·크래시가 나도 **여기까지 센 rep 은 기록 탭에 그대로 남음**. 비로그인 유저 한정 (Phase 1).
- **AI 코칭 샘플은 로그인 전에도 미리 본다**: 코칭 탭에 들어가면 로그인 전에도 샘플 피드백 2종이 먼저 보여서, 로그인할 가치가 있는지 확인 후 결정할 수 있음.
- **광고 없음 · 결제 없음**: MVP 단계는 전면 무료. 기본 카운팅·기록·자세 오버레이는 영구 무료 유지 계획.

### 타깃 시나리오

> "헬스장 3개월 등록했는데 2주 못 갔어요. PT는 회당 7~10만원, 유튜브 홈트는 내 폼이 맞는지 모르겠어요."

Mofit은 바로 이 지점을 겨냥합니다. 폰을 벽에 기대 세우고 손바닥을 카메라에 1초 보여주거나 화면을 탭하면 5초 카운트다운 후 추적이 시작됩니다. 스쿼트가 끝날 때까지 폰을 만지지 않아도 되고, 끝낼 때도 탭 또는 손바닥으로 세트 종료가 가능합니다.

### 현재 지원 범위 (솔직한 고지)

- **Mofit은 스쿼트 전용 카운터입니다.** 푸쉬업·런지·싯업·플랭크 등 다른 운동은 제공하지 않습니다.
- 홈 탭 시작 버튼은 "스쿼트 시작" 하나뿐이며, 운동 종류 선택 UI 자체가 없습니다 (ADR-017).
- 다른 운동 확장에 대한 일정은 약속하지 않습니다. 지금 설치하면 **스쿼트를 자동으로 세는 앱**으로 쓰게 됩니다.

### 솔직한 성능 고지

- 내부 개발 중 관측한 감각으로는 권장 환경(밝은 실내, 전신이 프레임에 들어오는 거리 2~3m, 일상복)에서 대체로 안정적으로 카운트됩니다. **다만 공개 가능한 정량 정확도 수치(예: 오카운트율 %, 조건별 미검출률)는 현재 확보되어 있지 않습니다.** 베타 이후 실제 사용 데이터로 측정해 구체 수치로 갱신할 계획.
- 조도 낮음 / 카메라 너무 가까움 / 하체 일부가 프레임 밖 등의 조건에서는 카운트가 튀거나 멈추는 경우가 있습니다. 이 경우 **§2.3의 진단 힌트 배너가 원인(프레임 아웃 / 저조도)을 인앱에서 안내**합니다.

## 2. 구체적 기능 설명 (미팅 / 데모 전달 시)

### 2.1 온보딩 (최초 1회, 약 3~4분)

성별 → 키 → 몸무게 → 체형(마른/보통/근육질/통통한) → 목표(체중 감량/근력 증가/체형 개선) → 코치 스타일. 총 6단계, 입력값 허용/경고 범위 검증 포함. 회원가입·이메일 요구 없음. 입력은 전부 폰 로컬(SwiftData)에 저장.

### 2.2 홈 탭

- 좌상단: "Mofit" 타이틀. 우상단: 프로필 편집 버튼.
- **"스쿼트 시작" 단일 버튼(형광초록)**. 탭 시 즉시 트래킹 화면 진입. 운동 종류 선택 UI 없음 (ADR-017).
- 오늘의 기록 요약: "세트 N · 스쿼트 N회 · N분" (기록 없으면 "첫 운동을 시작해보세요!")
- 운동 종료 후 복귀 시 폭죽 효과.

### 2.3 트래킹 화면 (카메라)

- **스쿼트 판정**: `VNDetectHumanBodyPoseRequest`로 hip-knee-ankle 각도 분석. 왼/오 다리 중 하나 기준. 서있음(>160°) → 앉음(<100°) → 서있음 = 1 rep.
- **손바닥 판정**: `VNDetectHumanHandPoseRequest`로 5개 손가락이 1초 펴진 상태 감지 → 세트 시작/종료 신호.
- **화면 탭 대체 트리거**: idle/tracking 상태에서 ZStack 전체 `.onTapGesture` 활성. 손바닥이 안 잡히는 각도·조도에서도 탭 한 번으로 동일 경로 실행. idle 서브카피: "손바닥을 보여주거나 화면을 탭하세요". tracking 서브카피: "끝낼 땐 화면을 탭하거나 손바닥을 보여주세요".
- **진단 힌트 오버레이 (ADR-018)**: 트래킹 시작 5초 유예 후, 양쪽(left/right) hip/knee/ankle 3조인트 모두 3초간 미검출이면 `.outOfFrame` 배너("전신이 프레임에 들어오는지 확인하세요 · 2~3m 거리 권장") 노출. 그 외 하체 조인트 평균 confidence < 0.5 가 3초 지속이면 `.lowLight` 배너("조명이 어두울 수 있어요 · 실내 조명을 밝혀주세요"). 우선순위 outOfFrame. 한 세션 내 rep 한 번 카운트되면 힌트 숨김 + 이후 재표시 금지. 배너는 반투명 상단 고정, 하단 종료 버튼 시야 가리지 않음.
- **rep 단위 autosave (ADR-009 2026-04-24 update, 신규)**: `countdown → tracking` 전이 시점에 빈 `WorkoutSession` 을 즉시 SwiftData insert + save. 이후 rep 이 증가할 때마다 해당 세션의 `repCounts` 배열에 스냅샷 append + `endedAt = .now` 갱신 + `try? modelContext.save()`. 세트 정상 종료(`completeSet`) 시 한 번 더 최종 save. 결과: 스쿼트 중 **전화 수신·앱 전환·OS 강제 종료·크래시**가 나도 "다음 번 앱 진입 시 기록 탭에 그때까지 센 rep 수가 남아있음". 비로그인 유저 한정 (Phase 1).
- 관절 포인트가 화면에 오버레이로 표시.
- 프리뷰 30fps, 분석 15fps 샘플링.

### 2.4 기록 탭

- 상단: 최근 7일 가로 스크롤 날짜바.
- 선택 날짜의 세션 리스트 (세트 수, 총 rep, 시간, 시작 시각).
- 스와이프 삭제.
- 기록 없는 날: "이 날은 운동 기록이 없어요".
- **0rep 세션은 숨김**: autosave 도입 부수효과로, rep 이 단 한 번도 카운트되지 않은 세션이 로컬 DB 에 남을 수 있음. 기록 탭 쿼리 단에서 `totalReps > 0` 메모리 필터로 자동 숨김 (사용자 스와이프 삭제 불필요).

### 2.5 AI 코칭 탭

**비로그인 분기 (설치 직후 바로 확인 가능)**: 로그인하지 않아도 코칭 탭에 진입하면 "로그인하면 이런 피드백을 받습니다" 섹션과 함께 **샘플 피드백 카드 2종**이 먼저 노출됩니다. 각 샘플은 **숫자 + 요일/시점 + 구체 행동 제안** 3요소를 포함:

- **샘플 A (운동 전 동기부여)**: *"지난 주 3일 운동 · 총 78회 스쿼트 · 일평균 26회. 수요일엔 38회 최다였고 금요일 0회였네요. 오늘은 수요일 페이스 회복해서 30회 이상 도전해보세요."*
- **샘플 B (운동 후 피드백)**: *"오늘 3세트 총 32회. 1세트 14회 → 2세트 10회 → 3세트 8회. 세트별 감소폭 4회로 피로도 자연스러운 곡선. 내일은 2세트째에서 쉬는 시간 20초 늘려보세요."*
- 각 카드 하단에 "※ 예시 피드백 (실제 데이터 기반으로 매번 다름)" 고지.

**로그인 분기 (활성)**: 하루 2회 호출(운동 전 동기부여 / 운동 후 피드백, 자정 리셋). **앱 → 자체 서버 `/coaching/request` → Anthropic Claude API** 경유 (API 키는 앱 바이너리에 없음). 전달 컨텍스트: 사용자 프로필 + 최근 7일 요약(운동일수, 총 세션, 총 rep, 일평균 rep) + 일별 추이(rep/세트/세트당 평균). API 실패 시 일일 횟수 차감 안 함. 서버는 DB 유일성 제약으로 1일 2회를 강제.

### 2.6 인증 (선택)

- 이메일 + 비밀번호 가입/로그인 (Supabase + JWT, Keychain 저장).
- 로그인 유저: 프로필/운동 기록/AI 피드백이 서버(Supabase)에도 저장.
- 비로그인 유저: 모든 데이터 로컬(SwiftData)에만 저장, 외부 전송 0.
- **주의**: 비로그인 상태로 쌓은 로컬 기록은 로그인 후 서버로 **이관되지 않음** (의도된 설계, ADR-013).

## 3. 가격 / 무료 범위 경계 (솔직한 고지)

### 단계 0 (현재, MVP)

- 전 기능 무료, 광고 없음.

### 단계 1 가설 (수개월 후, 미확정)

- **영구 무료로 유지하려는 기능**: 스쿼트 자동 카운팅, 세션 기록 저장/조회, 관절 각도 오버레이, 진단 힌트, rep 단위 autosave.
- **과금 후보 영역**: 하루 2회를 초과하는 AI 코칭 호출, 주간 리포트·장기 추이 분석, 커스텀 자세 가이드 — **아직 확정된 플랜 아님**.

## 4. 프라이버시 / 데이터

- 카메라 영상은 **어디에도 저장·전송되지 않음**. 온디바이스 Vision 분석 후 즉시 폐기.
- 비로그인: 모든 데이터 로컬. 로그인: Supabase 서버 저장 + Claude API 서버 프록시.
- API 키는 앱 바이너리에 없음 (서버 프록시).

## 5. 리스크 · 한계 · 아직 얕은 부분 (선제 고지)

현재 MVP 단계에서 **의도적으로 얕게 남긴 영역**입니다. 이 지점들은 "지금은 몰래 잘 되는 척" 하지 않고 미리 고지합니다.

1. **rep 카운트 정량 정확도 공개 미제공** — 권장 환경/비권장 환경에서의 수치(오카운트율 %, 미검출률) 확보 전. 베타 이후 측정해 갱신. 내부적으로도 실패 케이스 로깅 파이프라인이 아직 없음.
2. **진단 힌트는 2종으로 고정 (ADR-018)** — 현재 `.outOfFrame` / `.lowLight` 두 케이스만 커버. "카메라가 너무 가까움", "복장 때문에 하체 윤곽이 안 잡힘" 같은 세부 원인은 구분하지 않음. 2종의 임계치도 v1 경계값이며 실사용 데이터 누적 후 튜닝.
3. **스쿼트 외 운동은 영구적으로 제공되지 않을 수 있음** — MVP는 스쿼트 전용 포지셔닝이 확정됨(ADR-017). "홈트 앱"을 기대하고 설치하면 어긋남.
4. **앱스토어 출시 전 단계** — 별점·리얼 리뷰 등의 외부 신뢰 신호가 아직 없습니다. TestFlight 베타 배포도 준비 중. README에 스크린샷도 아직 없음. 현재 시점에서 공개 가능한 신뢰 신호는: (a) 이 문서 자체의 범위/한계 투명 고지, (b) 설치·사용 **비용이 0**이라 "망설이지 않고 설치해 직접 확인" 가능, (c) 카메라 영상 비저장/비전송 + API 키 서버 분리 등 프라이버시 설계 근거.
5. **비로그인 → 로그인 로컬 기록 미이관 (ADR-013)** — 비로그인 상태에서 쌓은 기록은 로그인해도 서버로 옮겨지지 않음. "3일 무료로 써서 쌓인 기록이 로그인 시 통째로 리셋되는 것처럼 보일 수 있다"는 유저 불신 이슈가 아직 남아 있음.
6. **rep autosave 는 비로그인 한정 (Phase 1 scope)** — 로그인 유저는 현행대로 "세트 정상 종료 시 서버 저장" 만 유지. 로그인 중 interruption 으로 rep 이 날아가는 케이스는 Phase 2 에서 서버/로컬 하이브리드 전략과 함께 재검토. 현재 로그인 유저에게는 "interruption 내성" 고지 안 함.
7. **rep autosave 는 세트 시작 이후만 방어** — 트래킹 화면 진입은 했지만 `countdown → tracking` 전이 전에 앱이 죽으면 여전히 기록 0. 전이 이전 구간(손바닥 1초 대기, 5초 카운트다운 중)은 autosave 대상 아님.
8. **AI 코칭 차별점 검증은 여전히 로그인 뒤** — 비로그인 상태 샘플 피드백 2종은 정적 하드코딩. 실제 Claude API 응답 퀄리티는 계정 생성 + 7일치 기록 축적 이후에만 확인 가능.

## 6. 설치 후 첫 10분 시나리오 (기대치 교정)

1. 설치 → 카메라 권한 허용 → 온보딩 3~4분
2. 홈 탭에서 "스쿼트 시작" 버튼 탭 → 카메라 뷰 진입
3. 폰을 벽에 기대거나 거치 → 전신이 프레임에 들어오도록 2~3m 거리
4. 손바닥을 카메라 방향으로 1초 들거나 **화면을 탭** → 5초 카운트다운 → 추적 시작
5. 스쿼트 수행 → rep이 올라가는 것을 확인. 프레임 밖이거나 조도가 낮으면 상단 배너로 원인 안내 (ADR-018). 중간에 전화·크래시가 나도 그때까지 센 rep 은 기록 탭에 자동 저장 (ADR-009 2026-04-24).
6. 끝낼 때 다시 손바닥 1초 또는 **화면 탭** → 세트 종료, 결과 화면
7. (선택) 코칭 탭에서 샘플 피드백 2종 확인 후 로그인 여부 결정

## 채택된 요구사항

- **run_id**: `home-workout-newbie-20s_20260424_224420`
- **title**: 비로그인 AI 코칭 샘플을 온보딩 값 + 로컬 기록 기반으로 **동적 생성** (CoachingSampleGenerator 도입)

### 유래한 고객 pain + 근거 인용

**페르소나** (`persuasion-data/personas/home-workout-newbie-20s/profile.md`):

- `risk_preference: conservative`, `trust_with_salesman: 40`, `personality_notes: "3일 써보고 아니면 삭제"`
- profile "시뮬 시 비판 포인트": *"'AI 코칭'이 일반론 수준이면 '그거면 ChatGPT 쓰지'로 결론."*
- `buy_triggers`: "촬영 없이 폰 세워두는 것만으로 자동 카운팅·피드백"

**시뮬 결과** (`run_id: home-workout-newbie-20s_20260424_224420`, keyman `decision: drop`, `confidence: 45`, 최종 판정 **실패**):

> **`01_keyman_initial.md` 라인 20 (구체적 이유 #4)**:
> "**AI 코칭 차별점은 로그인 + 7일 뒤에나 확인 가능**: §5-8 — 비로그인 샘플 2종은 하드코딩 정적 카피. 내가 실제로 받는 피드백 퀄리티는 '이메일·비밀번호 가입 + 7일간 꾸준히 운동'이라는 내 3일 컷 테스트보다 한참 먼 뒤에야 검증됨. '그거면 ChatGPT 쓰지' 결론 가능성 상존."

> **`report.md` 라인 45 (가치제안 개선 포인트 #4)**:
> "AI 코칭 차별점 검증 지연 (로그인 + 7일) — 대표 발화: '그거면 ChatGPT 쓰지' 결론 가능성 상존. 제안: 비로그인 샘플을 정적 하드코딩이 아니라 **온보딩 입력값(키/몸무게/목표) 기반으로 동적 생성**해서, 첫 10분 안에 'generic하지 않음'을 체감시켜라. 현재 §2.5의 2종 하드코딩 카드는 ChatGPT 대비 증거가 되지 못함."

이 pain 은 3일 컷 이탈 페르소나의 **설치 후 첫 10분** 구간에서 차별점 증거 부재로 이탈하는 것을 정면 겨냥. "ChatGPT 쓰지" 결론 가능성 제거는 Mofit AI 코칭 탭의 존재 정당성에 직결.

CTO 핵심 승인 근거: "Foundation-only pure struct + 1줄 치환이라 MVP 티켓 한 장 분량이며, 네트워크·인간개입 없음. 가치-비용 비가 명백."

### 구현 스케치

pain 의 본질: "내 입력값을 반영하지 않는 generic 문구". 2종 → 12종으로 늘리는 게 아니라 **온보딩 값을 문구에 직접 박아 ChatGPT 대비 증거 확보**.

**변경 범위 (Phase 1, 비로그인 유저 한정)**:

1. **`Mofit/Views/Coaching/CoachingSamples.swift` 교체**:
   - 기존 `enum CoachingSamples.all` static 배열 제거.
   - 신규 `struct CoachingSampleGenerator` (Foundation-only, pure struct, async/API 없음):
     ```swift
     struct CoachingSampleGenerator {
         static func generate(
             profile: UserProfile?,
             recentSessions: [WorkoutSession],  // 최근 7일
             now: Date
         ) -> [CoachingSample]  // 항상 pre 1개 + post 1개 반환
     }
     ```
   - 템플릿 차원: `goal`(3) × `기록유무`(2) = **6개 베이스 템플릿** (CTO 조건 2 반영). 요일/bodyType 은 문자열 인터폴레이션으로 처리.
   - 기록 0일 신규 유저 예: "여성 / 160cm / 체중감량 목표 기준, 첫 스쿼트는 10~15회로 시작해보세요. 수요일 저녁은 운동 수행률이 높은 시간대입니다."
   - 기록 1+일: 실제 로컬 `recentSessions` 의 총 rep/세션수/일평균/피크일 인터폴레이션.
   - 결정론적 (동일 입력 → 동일 출력, 테스트 가능).

2. **`Mofit/Views/Coaching/CoachingView.swift` 수정**:
   - 비로그인 분기에 `@Query` 로 `UserProfile` + 최근 7일 `WorkoutSession` 가져와 `CoachingSampleGenerator.generate(...)` 호출.
   - 라인 149 `ForEach(CoachingSamples.all)` → generator 결과로 치환.
   - **프로필 nil (온보딩 미완) 케이스는 카드 숨김 + "온보딩을 먼저 완료해주세요" CTA** (CTO 조건 4). 하드코딩 폴백 금지.
   - 카드 하단 고지: 현행 "※ 예시 피드백 (실제 데이터 기반으로 매번 다름)" **그대로 유지**. 로그인 유도 카피 추가 금지 (CTO 조건 3 반영).

3. **`MofitTests` 타겟 — `CoachingSampleGeneratorTests` 추가** (CTO 조건 1):
   - (profile=여성/160/55/감량, sessions=[]) → 생성 문자열에 "여성" OR "160" OR "감량" 중 하나 이상 포함 assert.
   - (profile 동일, sessions=[1세트 10회]) → 생성 문자열(post)에 "10" 포함 assert.
   - CI 통과 조건으로 설정. 실기기 QA 는 생략, AI 에이전트 자동 검증.

4. **`docs/adr.md` — ADR-006 아래 한 줄 주석 추가**: "2026-04-24: 비로그인 분기는 온보딩 값 + 로컬 세션 기반 결정론적 템플릿 생성으로 차별점 즉시 체감. 서버/Claude API 호출 없음."

5. **`docs/spec.md` — AI 코칭 탭 비로그인 분기 기술 섹션**: generator 입출력 + 프로필 nil 케이스 "온보딩 CTA 대체" 명시.

**Phase 1 에서 명시적으로 하지 않는 것**:

- 템플릿 개수 > 10 금지 (넘기면 재승인 요청).
- "로그인하면 Claude AI 기반 더 정교한 분석 가능" 카피 삽입 금지.
- 프로필 nil 폴백으로 기존 하드코딩 문자열 재사용 금지.
- 비로그인 분기에 서버/Claude API 호출 경로 추가 금지.
- 로그인 유저 경로 변경 금지.

**측정 계획 (Phase 2 트리거, 별건 티켓)**:

- Phase 1 배포 후 차기 iter 시뮬(`ideation` → `persuasion-review`) 에서 keyman 발화의 "그거면 ChatGPT 쓰지" 및 "하드코딩 정적 카피" 언급이 report.md 개선 포인트에서 제거되는지 확인.
- 템플릿 개수가 10개를 넘기면 그 시점에 재승인 요청 (CTO 조건 2).

**인간 개입 지점**: **없음** (CTO 조건 1). XCTest 자동화로 검증, 실기기 QA 생략.

### CTO 승인 조건부 조건

CTO 판정: **승인 / confidence 78**. 다음 4개 조건 이행 필수:

1. **(필수) 실기기 QA 필수화 금지**. `CoachingSampleGeneratorTests` XCTest 2케이스를 CI 통과 조건으로 설정: (a) (profile=여성/160/55/감량, sessions=[]) → "여성" OR "160" OR "감량" 중 하나 포함 assert, (b) (profile 동일, sessions=[1세트 10회]) → "10" 포함 assert. AI 에이전트 자동 검증으로 완결.
2. **(필수) 템플릿 확산 방지**. `goal(3) × 기록유무(2) = 6개 베이스 템플릿` 로 시작. 요일/bodyType 은 문자열 인터폴레이션으로 처리. 템플릿이 10개를 넘기면 그 시점에 재승인 요청.
3. **(필수) "로그인하면 Claude AI 기반 더 정교한 분석 가능" 카피 삽입 금지**. 비로그인 단계 로그인 유도 문구는 "이거 가짜구나"로 역효과. 현행 "※ 예시 피드백 (실제 데이터 기반으로 매번 다름)" 고지만 유지.
4. **(필수) 프로필 nil (온보딩 미완) 폴백**: 하드코딩 샘플 재사용 금지. 카드 자체를 숨기고 "온보딩을 먼저 완료해주세요" CTA 로 대체. 제거한 정적 카피가 폴백으로 남으면 요구사항의 목적이 무력화됨.

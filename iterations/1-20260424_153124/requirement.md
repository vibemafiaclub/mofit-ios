# Requirement

## 가치제안

---
run_id: home-workout-newbie-20s_20260424_153242
persona_id: home-workout-newbie-20s
persona_version: 1
delivery_mode: landing_plus_meeting
created_at: 2026-04-24T15:32:42
---

# Mofit — 폰 하나로 운동이 교정되는 AI 트레이너

## 1. 가치제안 요약 (랜딩페이지 카피)

### 메인 카피

**"폰 세워두고 손바닥 1초. 그게 전부입니다."**

iPhone 카메라만으로 스쿼트를 자동으로 세고, 자세를 실시간 교정합니다. 별도 장비도, 회원가입도 필요 없습니다. 앱 켠지 20초 안에 첫 스쿼트가 카운트됩니다.

### 서브 카피

- **자동 카운팅**: 폼이 맞을 때만 1회로 인정. 수기 체크 필요 없음.
- **개인화 AI 코칭**: 내 키·몸무게·목표와 최근 7일 운동 데이터 기반 피드백. 일반론 아닙니다.
- **오프라인 우선**: 로그인 없이 바로 시작. 데이터는 내 폰에만. (AI 코칭만 로그인 필요)
- **100% 무료 (MVP 단계)**: 광고 없음. 카운팅·기록은 영원히 무료.

### 타깃 시나리오

> "헬스장 3개월 등록했는데 2주 못 갔어요. PT는 비싸고, 유튜브 보고 따라해도 내 폼이 맞는지 모르겠어요."

Mofit은 바로 이 지점을 겨냥합니다. 폰을 벽에 기대 세워두고, 손바닥 1초를 카메라에 보여주면 5초 카운트다운 후 추적이 시작됩니다. 스쿼트가 끝날 때까지 폰에 손대지 않아도 됩니다.

---

## 2. 구체적 기능 설명 (미팅 / 데모 전달 시)

### 2.1 온보딩 (최초 1회, 약 30초)

성별 → 키 → 몸무게 → 체형(마른/보통/근육질/통통한) → 목표(체중 감량/근력 증가/체형 개선). 회원가입·이메일 요구 없음. 입력은 전부 폰 로컬(SwiftData)에 저장.

### 2.2 홈탭

- 오늘의 기록 요약 (세트, rep, 시간)
- "운동 시작" 형광초록 버튼
- 운동 종류 선택 (스쿼트 / 푸쉬업 / 런지 / 플랭크 — MVP에서는 내부적으로 전부 스쿼트로 처리, 향후 확장)

### 2.3 트래킹 화면 (카메라)

상태 머신:

```
대기 → 손바닥 1초 → 카운트다운 5초 → 운동 추적
     → 손바닥 1초 → 세트 완료 → 카운트다운 5초 → 다음 세트 → ... → 종료 버튼
```

- **스쿼트 판정**: Apple Vision Framework의 `VNDetectHumanBodyPoseRequest` 로 hip-knee-ankle 각도 분석. 각도 > 160°일 때 서있음, < 100°일 때 앉음. 서있음 → 앉음 → 서있음 = 1 rep.
- **손바닥 판정**: `VNDetectHumanHandPoseRequest` 로 5개 손가락이 1초 펴진 상태 감지 → 세트 시작/종료 신호.
- 관절 포인트가 화면에 오버레이로 표시되어 자세 교정 가능.
- 프리뷰 30fps, 분석 15fps 샘플링 (배터리 절약 + 스쿼트 속도에 충분).

### 2.4 기록탭

- 상단: 최근 7일 가로 스크롤 날짜바
- 선택 날짜의 세션 리스트 (운동 종류, 세트수, 총 rep, 시간, 시작 시각)
- 스와이프 삭제

### 2.5 AI 코칭탭 (로그인 필요)

- 하루 2회: 운동 전 동기부여 / 운동 후 피드백 (자정 리셋)
- Claude Sonnet 4.6 모델 사용, 서버에서 프록시
- 전달 컨텍스트: 사용자 프로필 + 최근 7일 요약(운동일수, 총세션, 총rep, 일평균rep) + 일별 추이(rep/세트/세트당 평균)
- API 실패 시 횟수 차감 안 함

### 2.6 프라이버시 / 데이터

- **비로그인**: 모든 데이터 로컬(SwiftData). 외부 전송 0.
- **로그인 시**: 프로필/운동기록/AI 피드백이 서버(Supabase, JWT 인증)에 저장. Claude API도 서버 경유.
- 카메라 영상은 어디에도 저장·전송되지 않음. 실시간 분석 후 폐기.

---

## 3. 가격 / 플랜

### 단계 0 (현재, MVP)

**완전 무료. 광고 없음. 회원가입 없음.** 기본 기능(카운팅·기록·자세 오버레이)은 영구 무료로 유지할 계획.

### 단계 1 (가설, 수개월 후)

- 기본 카운팅·기록·자세 오버레이: 영구 무료
- 프리미엄 (가설): AI 코칭 호출 횟수 상향 + 주간 리포트 + 운동별 폼 점수 상세

단계 1 BM은 사용자 수·리텐션 검증 후 확정 예정. 지금 설치하는 사용자는 "완전 무료 MVP 단계"를 확약받는 것과 같음.

### 단계 2 (가설, 장기)

B2B — 헬스장/PT 숍용 고객 운동 데이터 대시보드. 개인 사용자에게는 영향 없음.

---

## 4. 경쟁 대안 대비 포지션

| 대안                   | 강점                        | 약점                                    | Mofit의 답                        |
| ---------------------- | --------------------------- | --------------------------------------- | --------------------------------- |
| Apple Fitness+         | 영상 퀄리티, Apple 통합     | 자세 피드백 없음, 월 구독료             | 무료 + 자세 실시간 교정           |
| YouTube 홈트 영상      | 무료, 다양성                | 피드백 0, 몰입 끊김                     | 자동 카운팅으로 운동만 집중       |
| 헬스장 + PT            | 실제 피드백, 강제성         | 회당 7~10만원, 이동 부담                | 폰만 있으면 됨, 무료              |
| 무료 홈트 카운터 앱    | 무료                        | 수동 카운팅, 자세 인식 없음             | 자동 카운팅 + AI 자세 분석        |

---

## 5. 리스크 (솔직한 고지)

- **스쿼트 외 운동**은 UI는 있으나 MVP에서는 내부적으로 스쿼트 판정 로직을 재사용. 푸쉬업·런지·플랭크의 정확한 판정은 단계적으로 추가 예정.
- **rep 카운트 정확도**: Apple Vision 기반이므로 조명·각도·옷차림에 따라 오차 가능. 폰은 발끝에서 약 2m, 허리 높이에 세워두는 것이 권장 환경.
- **iOS 17+ 전용** (iPhone XS 이상). Android·iPad 미지원.

---

## 6. 즉시 시작하는 방법

1. App Store에서 "Mofit" 검색 (출시 예정)
2. 앱 실행 → 온보딩 30초 → 홈 → 운동 시작
3. 폰을 벽에 기대 세우기 → 화면 안에 서서 손바닥을 1초 펴기
4. 5초 후 카운트 시작. 끝나면 손바닥 1초 더 → 세트 완료.

설정도, 로그인도, 결제도 없습니다. 카메라 권한만 허용하면 끝.

## 채택된 요구사항

- **run_id**: `home-workout-newbie-20s_20260424_153242`
- **title**: 스쿼트 외 운동 버튼 "준비중" 표시 — 기대 불일치로 인한 3일 내 삭제 경로 차단

### 유래한 고객 pain + 근거 인용

**페르소나** (`persuasion-data/personas/home-workout-newbie-20s/profile.md`):

- **reject_trigger**: "실제로 써보니 rep 카운트가 자주 틀림 → 신뢰도 붕괴"
- **시뮬 비판 포인트**: *"스쿼트만 제대로 되고 나머지 운동(푸쉬업/런지/플랭크) UI만 있고 내부는 스쿼트 통일이라는 점을 알게 되면 실망 가능성 있음"*
- **구매 맥락**: "3일 써보고 아니면 삭제"

**시뮬 결과** (keyman `decision: drop`, `confidence: 62`):

> **01_keyman_initial.md (라인 17)**:
> "가치제안 5번(리스크)에 'UI는 있으나 내부적으로 스쿼트 판정 로직을 재사용'이라고 솔직히 써 있다. 투명성은 가점이지만, 내 시뮬 비판 포인트에 '스쿼트만 제대로 되고 나머지가 UI뿐임을 알게 되면 실망'이라고 명시되어 있다. 3일 쓰는 동안 내가 푸쉬업 한 번 눌러보면 바로 들통난다 = **즉시 삭제 경로**."

**report.md** 가치제안 개선 포인트 #1 — keyman drop 3대 사유 중 **1순위**.

### 구현 스케치

1. **`Mofit/Views/Home/ExercisePickerView.swift` 수정** — 스쿼트만 active, 나머지 2종(푸쉬업/싯업)은 다음 처리:
   - 셀 스타일 비활성화 톤 (opacity 0.4, 회색)
   - "준비중" 배지
   - tap 시 운동 시작 대신 토스트/haptic 피드백 (tap 자체는 막지 않음 — 향후 재활성화 시 회귀 테스트 누락 방지 목적, CTO 조건부 #4)
   - 토스트 카피: **"현재는 스쿼트만 지원합니다"** (미래 약속 문구 금지, CTO 조건부 #5)

2. **ADR 신규 항목** (`docs/adr.md`) — ADR-008을 직접 수정하지 않고 신규 항목(예: ADR-016) 추가. 근거로 `run_id: home-workout-newbie-20s_20260424_153242` 한 줄만 인용, 길게 쓰지 않음 (CTO 조건부 #3).

3. **문서 드리프트 정리** — `docs/prd.md`, `docs/spec.md`의 운동 종목 기재를 실코드(`ExercisePickerView.swift`: 스쿼트/푸쉬업/싯업 3종) 기준으로 일치시킨다 (CTO 조건부 #1). 페르소나 시뮬·value_proposition에 쓰인 "런지/플랭크"는 PRD 구버전 기재이며 실제 뷰에는 없음.

4. **xcodegen + xcodebuild 로 빌드 검증.** SwiftData 스키마 변경 0, 서버 API 변경 0. 인간 개입 지점: App Store 배포만 수동.

### CTO 승인 조건부 조건

CTO 판정: **승인 / confidence 78**. 다음 5개 조건 이행 필수:

1. **실코드 기준으로 재확인**. `ExercisePickerView.swift`의 실제 종목은 "스쿼트/푸쉬업/싯업" 3종이며 "런지/플랭크"는 존재하지 않음. 제안서가 PRD("런지/플랭크")를 인용해 혼동을 유발했음. 작업은 실코드 기준으로 하고, `docs/prd.md`·`docs/spec.md`의 종목 기재를 실코드와 맞게 수정하여 문서-코드 드리프트도 같이 정리.

2. **Analytics 이벤트(`exercise_locked_cta_tapped`)는 이번 티켓에서 제외**. 코드베이스에 Mixpanel/Analytics 호출이 아직 없음(ADR-015 결정만 존재, 실장 전). analytics는 별도 티켓에서 Mixpanel SDK 도입 + 기본 이벤트 세트와 함께 처리.

3. **ADR는 ADR-008을 수정하지 말고 신규 항목 추가**. 근거로 시뮬 run_id 한 줄만 인용, 길게 쓰지 말 것. ADR-008은 히스토리로 보존.

4. **"준비중" 셀의 tap 자체는 막지 말 것**. 토스트/피드백만 띄우는 방식. 완전 disabled는 향후 재활성화 시 회귀 테스트 누락 리스크.

5. **토스트 카피에 미래 약속 금지**. "곧 지원됩니다" 같은 문구는 `trust_with_salesman: 40` 페르소나가 뒤집어 읽는 성향이 있으므로 "현재는 스쿼트만 지원합니다" 수준의 사실 진술로.

---

**영향 파일 목록**:
- `Mofit/Views/Home/ExercisePickerView.swift`
- `docs/adr.md`
- `docs/prd.md`
- `docs/spec.md`
- 시뮬 레퍼런스: `persuasion-data/runs/home-workout-newbie-20s_20260424_153242/report.md`, `01_keyman_initial.md`
